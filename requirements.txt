# --- Runtime (the app) ---
fastapi
uvicorn
pydantic-settings
python-jose[cryptography]
bcrypt
psycopg2-binary
langgraph            # pulls langchain-core
openai
langfuse
chromadb             # embeddings via Chroma's built-in ONNX MiniLM
pyyaml

# --- Scripts (seeding) ---
pandas
numpy

# --- FAQ store build only (create_vectordb.py) ---
datasets
tqdm

# --- Guardrails (input safety; then run scripts/fetch_models.py for the non-pip models) ---
presidio-analyzer
presidio-anonymizer
huggingface_hub      # used by scripts/fetch_models.py for the ONNX injection classifier
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# --- Tests & golden-set eval (local dev) ---
pytest
httpx                # FastAPI TestClient
deepeval
