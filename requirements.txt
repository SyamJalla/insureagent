# --- Runtime (the app) ---
fastapi
uvicorn
pydantic-settings
python-jose[cryptography]
bcrypt
psycopg2-binary
langgraph            # pulls langchain-core
openai
langfuse
chromadb             # embeddings via Chroma's built-in ONNX MiniLM
pyyaml

# --- Scripts (seeding) ---
pandas
numpy

# --- FAQ store build only (create_vectordb.py) ---
datasets
tqdm

# --- Guardrails (input safety; spaCy model: python -m spacy download en_core_web_sm) ---
presidio-analyzer
presidio-anonymizer
huggingface_hub      # fetches the ONNX injection classifier (cached on first use)

# --- Tests & golden-set eval (local dev) ---
pytest
httpx                # FastAPI TestClient
deepeval
